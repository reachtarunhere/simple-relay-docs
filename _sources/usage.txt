Usage
=====

Simple Relay allows the user to select the port to run on and by default uses port 9999.

Example: python simplerelay.py 9800

After this 2 clients need to connect to the server and send/recieve data. 